apiVersion: v1
kind: Service
metadata:
  name: {{ include "mcp-gateway.fullname" . }}
spec:
  selector:
    app: {{ include "mcp-gateway.name" . }}
  ports:
  - protocol: TCP
    port: {{ .Values.service.port }}
    targetPort: 8080
